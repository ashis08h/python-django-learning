Constructor is a method inside a class which will automatically invoked(called) when
we create an object of a class. In python we use dunder method to create a constructor __init__.
constructor will not return anything except None.

when we create an object with parameters then constructor will help us to define those
parameters in the class.

There are two types of constructor in oops

1) Parameterized constructor
2) Default constructor

Parameterized constructor - when we pass argument from an object then we use parameterized constructor.
Default constructor - When we do not pass any argument from an object then we use
default constructor.